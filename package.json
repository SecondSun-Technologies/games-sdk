{
    "name": "games-sdk-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "typecheck": "npm -ws run typecheck --if-present",
        "build": "npm -ws run build --if-present",
        "lint": "npm -ws run lint --if-present",
        "test": "npm -ws run test --if-present",
        "clean": "npm -ws run clean --if-present"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}